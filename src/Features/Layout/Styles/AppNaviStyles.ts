import { makeStyles } from "@fluentui/react-components";

export const useAppNaviStyles = makeStyles({
    root: {
        height: "100%",
    },
    inSideBarRoot: {},
    tabList: {
        height: "100%",
    },
    inSideBarItem: {},
});
